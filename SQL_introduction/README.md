# SQL Introduction

This project covers the basics of SQL and MySQL:
databases, tables, DDL/DML statements, SELECT/INSERT/UPDATE/DELETE,
subqueries, and MySQL functions like COUNT, AVG, and GROUP BY.

Tasks were completed using MySQL 8.0 on Ubuntu 20.04 LTS.

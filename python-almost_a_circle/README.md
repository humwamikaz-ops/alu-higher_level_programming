# Python - Almost a Circle

This project builds an OOP hierarchy in Python: a Base class managing
unique ids, a Rectangle class inheriting from it with full attribute
validation, area/display/update/dictionary/JSON methods, and a Square
class inheriting from Rectangle that reuses all of its logic with a
single size attribute.

All classes and methods are unit tested (see tests/) and PEP 8 validated.

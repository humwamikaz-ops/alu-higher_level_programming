# SQL More Queries

This project builds on SQL basics with users, privileges, constraints
(NOT NULL, DEFAULT, UNIQUE, PRIMARY KEY, FOREIGN KEY), and multi-table
queries using JOIN and subqueries against the hbtn_0d_usa and
hbtn_0d_tvshows databases.

Tasks were completed using MySQL 8.0 on Ubuntu 20.04 LTS.

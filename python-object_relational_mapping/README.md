# Python - Object Relational Mapping

This project covers connecting Python to a MySQL database, first using
raw SQL via MySQLdb (including SQL injection risks and how to prevent
them with parameterized queries), then using the SQLAlchemy ORM to
define, query, insert, update, and delete State and City model objects.
